import { resultBadge } from 'editors-lab/helpers/result-badge';
import { module, test } from 'qunit';

module('Unit | Helper | result badge');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = resultBadge([42]);
  assert.ok(result);
});
