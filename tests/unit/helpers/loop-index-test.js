import { loopIndex } from 'editors-lab/helpers/loop-index';
import { module, test } from 'qunit';

module('Unit | Helper | loop index');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = loopIndex([42]);
  assert.ok(result);
});
