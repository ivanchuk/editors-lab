import { videoMeta } from 'editors-lab/helpers/video-meta';
import { module, test } from 'qunit';

module('Unit | Helper | video meta');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = videoMeta([42]);
  assert.ok(result);
});
