import { buttonColor } from 'editors-lab/helpers/button-color';
import { module, test } from 'qunit';

module('Unit | Helper | button color');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = buttonColor([42]);
  assert.ok(result);
});
