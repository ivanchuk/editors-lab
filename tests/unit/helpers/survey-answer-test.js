import { surveyAnswer } from 'editors-lab/helpers/survey-answer';
import { module, test } from 'qunit';

module('Unit | Helper | survey answer');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = surveyAnswer([42]);
  assert.ok(result);
});
